# project title --> Blog Application
 Build a fully functional blog application with a Post model (title, body, 
author, published date, status). Implement list and detail views, a working admin panel 
with search and filters, URL routing with named paths, a base template with template 
inheritance, and static file loading. All data must be stored and queried through the ORM 
— no raw SQL. 