# project title --> Books API
 Build a fully functional REST API for a library system. It must expose 
endpoints for books and authors with full CRUD using ModelViewSet and 
DefaultRouter. Nested serializers must show author details inside book responses. 
Token authentication must protect all write operations while read operations remain 
public. Test all endpoints through the browsable API and verify token auth using a tool 
like Postman or curl.