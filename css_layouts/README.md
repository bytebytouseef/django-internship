# project title --> Responsive Dashboard Layout
## description 
Build a fully responsive admin dashboard page from 
scratch using only HTML and CSS. The layout must use Grid for the overall page 
structure and Flexbox for component-level layouts. It must include a sidebar, a top 
navbar, a stats card row, and a data table section. The page must reflow cleanly at 
mobile, tablet, and desktop breakpoints using media queries. 