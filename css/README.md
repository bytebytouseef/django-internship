# project title --> Styled Intern Profile Page
## description
Take the HTML page from Day 1 and apply full CSS styling. 
The page must use an external stylesheet, demonstrate at least five selector types, 
implement the box model consciously on every section, use CSS variables for the colour 
palette, and include hover effects on the nav links and form button — no frameworks. 